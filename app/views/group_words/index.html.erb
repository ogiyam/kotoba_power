<h1>GroupWards#index</h1>

<% @group_words.each do |group| %>
   <%= group.word %>
   <% if @groups.owner_id == current_user.id %>
     <%= link_to "削除", group_groupword_path(@groups,group), method: :delete %>
   <% end %>
<% end %>

<%= link_to "ことば作成", new_group_groupword_path %>

