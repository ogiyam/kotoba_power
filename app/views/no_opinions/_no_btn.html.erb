<% if group_word.no_opinions_by?(current_user) %>
    <%= link_to '反対ではない', group_group_word_no_opinions_path(group_word_id: group_word.id), method: :delete, remote: true, data: {"turbolinks" => false} %>
  <% else %>
    <%= link_to '反対', group_group_word_no_opinions_path(group_word_id: group_word.id), method: :post, remote: true,  data: {"turbolinks" => false} %>
<% end %>